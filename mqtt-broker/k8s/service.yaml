apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  namespace: mqtt-broker
spec:
  selector:
    app: mosquitto
  type: ClusterIP
  ports:
    - name: mqtt
      port: 1883
      targetPort: 1883
